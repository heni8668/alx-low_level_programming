#include "holberton.h"
/**
* add - adds two numbers together.
* @i: the first number is added.
* @j: the second number is added.
* Return: the sum of two numbers.
*/
int add(int i, int j)
{
return (i + j);
}
